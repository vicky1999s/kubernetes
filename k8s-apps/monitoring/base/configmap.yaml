apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 10s
      external_labels:
        cluster: kind
    scrape_configs:
    - job_name: prometheus
      static_config:
        targets:
          - "localhost:9090"
